<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="MDDevDaysApp.Views.InfoPage"
             BackgroundColor="{DynamicResource WindowBackground}"
             Title="{Binding Title}">
    <ContentPage.Icon>
        <OnPlatform x:TypeArguments="FileImageSource">
            <On Platform="iOS" Value="Info.png" />
        </OnPlatform>
    </ContentPage.Icon>


    <StackLayout HorizontalOptions="Center" Margin="0,10,0,0">
        <Image Source="{Binding BannerSource}" />
        <Label Text="{Binding ConferenceName}" HorizontalTextAlignment="Center" />
        <Label Text="{Binding ConferenceDate}" HorizontalTextAlignment="Center" />
        <Label Text="{Binding ConferenceLocation}" HorizontalTextAlignment="Center" />
    </StackLayout>
</ContentPage>